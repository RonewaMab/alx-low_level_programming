#include <stdio.h>

/**
 * main-prints the phrase with proper grammar,
 * but the out put is piece of art
 * Return:0 if exited properly,non-zero otherwise
 */
int main(void)
{
printf("with propergramma,but the outcome is a piece of art,"\n);
return (0);
}
